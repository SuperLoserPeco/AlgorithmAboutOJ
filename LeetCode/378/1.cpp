#include <vector>
#include <string>
#include <iostream>
#include <map>
using namespace std;

// fang qi
class Solution {
public:
    int kthSmallest(vector<vector<int>>& matrix, int k) {
        int n = matrix.size();
        int part = (n + 1) * n / 2;
        if(k < part) {

        }   
        else {
        } 
    }
};